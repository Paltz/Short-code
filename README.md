# Welcome to Short-code Vite Project 👋

## Get started

1. Install dependencies

   ```bash
   npm install then open file of backend `cd backend` then npm install again
   ```

2. Start the app

   ```bash
    npm run start
   ```
